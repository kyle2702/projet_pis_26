# Exemple de fichier d'environnement pour les Cloud Functions (NE PAS COMMITTER VOS VALEURS RÉELLES)
# Copiez ce fichier en .env (qui doit être ignoré par git) et remplissez la clé.
# ATTENTION: Sur le plan Spark, il n'y a pas de Secret Manager. La variable doit être fournie au moment du build/deploy.
# Options:
#  - Windows PowerShell:  $env:BREVO_API_KEY="votre_cle"; npm run build; firebase deploy --only functions:onJobCreated
#  - Bash: export BREVO_API_KEY="votre_cle" && npm run build && firebase deploy --only functions:onJobCreated
#  - Ou utiliser un fichier .env + une lib de chargement local (non nécessaire en production firebase)

BREVO_API_KEY=VOTRE_CLE_ICI
